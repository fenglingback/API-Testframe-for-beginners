# from nb_log import LogManager



# class MyLogger:



#     def __init__(self, is_stream: bool = True, file_name: str = None) -> None:
#         if file_name is not None:
#             self.log_filename = file_name
#             self.logger = LogManager().get_logger_and_add_handlers(is_add_stream_handler=is_stream, log_filename=self.log_filename)
#         else:
#             self.logger = LogManager().get_logger_and_add_handlers()

        



# console_logger = MyLogger().logger

